# ==========================================
# COMP 653 Finance Project - Requirements
# ==========================================

# 1. Data Acquisition 
yfinance>=0.2.0           # High-frequency S&P 500 OHLCV data
fredapi>=0.5.0            # Macroeconomic indicators (CPI, oil prices, etc.)

# 2. Data Processing & Feature Engineering
pandas>=2.0.0             # Data manipulation and time-series alignment
numpy>=1.24.0             # Numerical operations and array handling
pyarrow>=12.0.0           # Parquet file support for fast data loading from Drive

# 3. Chart Pattern Recognition
# zeta-zetra/chart_patterns is deliberately NOT listed here: `pip install git+...`
# fails to resolve, and because pip resolves everything before installing anything,
# it silently aborts this entire file (ray, optuna and fredapi never install).
# Its metadata is broken four ways:
#   - pins kaleido==0.1.0.post1, a Windows-only build (unavailable on Linux)
#   - requires Python <3.11, but Colab runs 3.12
#   - pins pandas==1.3.5, contradicting this project's pandas>=2.0.0
#   - its modules import `chart_patterns.chart_patterns.*`, a nested layout its
#     own flat install never creates, so the installed package cannot import
# The detector code itself is fine and runs on modern pandas. Clone it and put
# the clone's PARENT directory on sys.path instead:
#     !git clone https://github.com/zeta-zetra/chart_patterns.git /content/vendor/chart_patterns
#     sys.path.append('/content/vendor')
#     from chart_patterns.chart_patterns.pivot_points import find_all_pivot_points
# Its real runtime deps (plotly, scipy, tqdm) are preinstalled on Colab.

# 4. Machine Learning & Modeling 
scikit-learn>=1.2.0       # Data scaling (StandardScaler) and evaluation metrics
torch>=2.0.0              # PyTorch framework for Baseline and Augmented RNNs
joblib>=1.2.0             # Persisting the fitted scaler alongside model weights

# 5. Evaluation & Visualization
matplotlib>=3.7.0         # Core plotting library for evaluation
seaborn>=0.12.0           # Statistical data visualization (loss curves, etc.)

# 6. Hyperparameter Tuning (Colab: Python <=3.12; no wheels for 3.13 yet)
ray[tune]>=2.43.0         # Parallel trials + ASHA; 2.43 exports RunConfig etc.
                          # from ray.tune (ray.train's Train V2 versions break Tuner)
optuna>=3.4.0             # TPE Bayesian search algorithm for Ray Tune
